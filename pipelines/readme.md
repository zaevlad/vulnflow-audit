# `pipelines` folder

Saved **pipeline scenarios** from the VulnFlow web UI: the node graph (agents, memory, code, patterns, etc.), edges, scenario name, path to the audited project, and excluded paths.

Files are JSON (`<name>.json`). You can version them in Git, copy them between machines, and reopen them in the builder from the saved-pipelines list.
