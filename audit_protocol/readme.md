# `audit_protocol` folder

Place **the protocol’s source repository** here — the codebase you are going to audit with VulnFlow. Keeping it under this directory makes it clear which project the pipeline targets and keeps audit assets next to the builder.

Keep RAG documentation in `audit_docs/` and long-form agent notes in `memory/` as described in the main project docs.
