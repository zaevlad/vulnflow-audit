memory_promts folder
====================

Purpose: instruction texts (prompts) for the model in the Memory block — how to process accumulated context and what to write into files under memory/.

Important: all working prompt files in this folder must be Markdown only — use the .md extension.

This readme.txt describes the folder; put each prompt in its own .md file.
